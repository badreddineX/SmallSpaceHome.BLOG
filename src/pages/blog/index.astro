---
import { getCollection } from 'astro:content';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import SEO from '../../components/SEO.astro';
import '../../styles/global.css';

const allPosts = await getCollection('blog');
const sorted   = allPosts.sort(
  (a, b) => new Date(b.data.datePublished).getTime() - new Date(a.data.datePublished).getTime()
);

const categories = ['All', 'Organization', 'Decor', 'Storage', 'Budget Tips'];

const catClass: Record<string, string> = {
  'Organization': 'cat-organization',
  'Decor':        'cat-decor',
  'Storage':      'cat-storage',
  'Budget Tips':  'cat-budget-tips',
};

const POSTS_PER_PAGE = 6;
---

<!doctype html>
<html lang="en-CA">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <SEO
      title="Blog — SmallSpace Home"
      description="All posts on organization, decor, storage, and budget tips for Canadian apartment renters. Browse by category or start from the top."
      type="website"
    />
  </head>
  <body>
    <Header />

    <main>
      <!-- Editorial hero -->
      <section class="read-hero">
        <h1 class="read-hero-title">
          Read All<br />
          <em>Organization &amp; Decor</em>
        </h1>
      </section>

      <!-- Keyword search bar -->
      <div class="search-bar-wrap">
        <div class="search-bar-inner">
          <label for="post-search" class="sr-only">Search posts</label>
          <div class="search-field">
            <svg class="search-icon" width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
              <circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/>
            </svg>
            <input
              type="search"
              id="post-search"
              class="search-input"
              placeholder="Search by keyword — organization, IKEA, budget…"
              autocomplete="off"
              spellcheck="false"
            />
            <button class="search-clear" id="search-clear" aria-label="Clear search" hidden>
              <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
                <line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/>
              </svg>
            </button>
          </div>
        </div>
      </div>

      <!-- Category filter -->
      <div class="filter-bar" id="filter-bar">
        <nav class="filter-tabs" role="tablist" aria-label="Filter by category">
          {categories.map(cat => (
            <button
              class={`filter-tab${cat === 'All' ? ' active' : ''}`}
              data-category={cat}
              role="tab"
              aria-selected={cat === 'All' ? 'true' : 'false'}
            >
              {cat}
            </button>
          ))}
        </nav>
      </div>

      <!-- Posts grid -->
      <section class="posts-section">
        <div class="posts-grid" id="posts-grid">
          {sorted.map(post => {
            const date = new Date(post.data.datePublished).toLocaleDateString('en-CA', {
              year: 'numeric', month: 'long', day: 'numeric',
            });
            return (
              <article
                class="post-item"
                data-category={post.data.category}
                data-title={post.data.title.toLowerCase()}
                data-desc={post.data.description.toLowerCase()}
              >
                <a href={`/blog/${post.slug}`} class="post-img-link">
                  <div class="post-img-wrap img-placeholder">
                    <img
                      src={post.data.image}
                      alt={post.data.title}
                      loading="lazy"
                      width="800"
                      height="1066"
                      onerror="this.parentElement.classList.add('img-error')"
                    />
                  </div>
                </a>
                <div class="post-info">
                  <span class={`post-cat ${catClass[post.data.category] ?? 'cat-organization'}`}>
                    {post.data.category}
                  </span>
                  <h2 class="post-title">
                    <a href={`/blog/${post.slug}`}>{post.data.title}</a>
                  </h2>
                  <p class="post-meta">
                    <time datetime={post.data.datePublished}>{date}</time>
                    <span aria-hidden="true">·</span>
                    <span>{post.data.readTime} min read</span>
                  </p>
                </div>
              </article>
            );
          })}
        </div>

        <p class="no-results" id="no-results" hidden>
          No posts in this category yet — check back soon!
        </p>

        <!-- Pagination controls -->
        <div class="pagination" id="pagination" aria-label="Pagination">
          <button class="page-btn" id="prev-btn" aria-label="Previous page" disabled>
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
              <polyline points="15 18 9 12 15 6"/>
            </svg>
            <span>Previous</span>
          </button>

          <div class="page-numbers" id="page-numbers"></div>

          <button class="page-btn" id="next-btn" aria-label="Next page">
            <span>Next</span>
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
              <polyline points="9 18 15 12 9 6"/>
            </svg>
          </button>
        </div>

        <!-- Post count info -->
        <p class="page-info" id="page-info" aria-live="polite"></p>
      </section>
    </main>

    <Footer />
  </body>
</html>

<style>
/* ── Hero ── */
.read-hero {
  text-align: center;
  padding: 80px 24px 56px;
  background: var(--color-white);
  border-bottom: 1px solid var(--color-border);
}

.read-hero-title {
  font-family: var(--font-display);
  font-size: clamp(2.4rem, 6vw, 4.2rem);
  font-weight: 400;
  line-height: 1.12;
  color: var(--color-heading);
  margin: 0;
}

.read-hero-title em {
  font-style: italic;
  font-weight: 400;
  color: var(--color-heading);
}

/* ── Search bar ── */
.sr-only {
  position: absolute; width: 1px; height: 1px;
  padding: 0; margin: -1px; overflow: hidden;
  clip: rect(0,0,0,0); white-space: nowrap; border: 0;
}

.search-bar-wrap {
  background: var(--color-white);
  border-bottom: 1px solid var(--color-border);
  padding: 10px 24px;
}

.search-bar-inner {
  max-width: 560px;
  margin: 0;
}

.search-field {
  display: flex;
  align-items: center;
  gap: 7px;
  border: 1px solid var(--color-border);
  border-radius: 4px;
  padding: 7px 12px;
  transition: border-color var(--transition);
}
.search-field:focus-within { border-color: var(--color-ink); }

.search-icon {
  color: var(--color-text-faint);
  flex-shrink: 0;
  width: 13px;
  height: 13px;
  transition: color var(--transition);
}
.search-field:focus-within .search-icon { color: var(--color-text-muted); }

.search-input {
  flex: 1;
  background: none;
  border: none;
  outline: none;
  font-family: var(--font-body);
  font-size: 0.72rem;
  color: var(--color-ink);
  min-height: 28px;
  -webkit-appearance: none;
  letter-spacing: 0.02em;
}
.search-input::placeholder { color: var(--color-text-faint); font-size: 0.73rem; }
.search-input::-webkit-search-cancel-button { display: none; }

.search-clear {
  background: none;
  border: none;
  cursor: pointer;
  color: var(--color-text-faint);
  padding: 2px;
  display: flex;
  align-items: center;
  transition: color var(--transition);
  -webkit-tap-highlight-color: transparent;
}
.search-clear:hover { color: var(--color-ink); }

/* ── Filter bar ── */
.filter-bar {
  background: var(--color-white);
  border-bottom: 1px solid var(--color-border);
  position: sticky;
  top: var(--header-height);
  z-index: 50;
  padding: 0 24px;
}

.filter-tabs {
  display: flex;
  align-items: center;
  gap: 0;
  overflow-x: auto;
  scrollbar-width: none;
  max-width: var(--wide-width);
  margin: 0 auto;
}
.filter-tabs::-webkit-scrollbar { display: none; }

.filter-tab {
  background: none;
  border: none;
  border-bottom: 2px solid transparent;
  padding: 16px 22px;
  font-family: var(--font-body);
  font-size: 0.82rem;
  font-weight: 400;
  letter-spacing: 0.04em;
  color: var(--color-text-muted);
  cursor: pointer;
  white-space: nowrap;
  transition: color var(--transition), border-color var(--transition);
  -webkit-tap-highlight-color: transparent;
  min-height: var(--touch-min);
}
.filter-tab:hover { color: var(--color-ink); }
.filter-tab.active {
  color: var(--color-ink);
  border-bottom-color: var(--color-ink);
  font-weight: 500;
}

/* ── Posts grid ── */
.posts-section {
  padding: 56px 24px 80px;
  max-width: var(--wide-width);
  margin: 0 auto;
}

.posts-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 40px 28px;
}

.post-item { display: none; }
.post-item.visible { display: block; }

/* Image */
.post-img-link { display: block; overflow: hidden; }

.post-img-wrap {
  aspect-ratio: 3 / 4;
  overflow: hidden;
  background: var(--color-surface);
}
.post-img-wrap img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: transform 0.6s ease;
  display: block;
}
.post-item:hover .post-img-wrap img { transform: scale(1.04); }

/* Broken image fallback */
.post-img-wrap.img-error img { display: none; }
.post-img-wrap.img-error {
  display: flex;
  align-items: center;
  justify-content: center;
}
.post-img-wrap.img-error::after {
  content: '';
  display: block;
  width: 40px;
  height: 40px;
  background: var(--color-border);
  border-radius: 50%;
}

/* Text below image */
.post-info {
  padding: 16px 0 0;
  display: flex;
  flex-direction: column;
  gap: 6px;
}

.post-cat {
  font-family: var(--font-body);
  font-size: 0.6rem;
  font-weight: 500;
  letter-spacing: 0.16em;
  text-transform: uppercase;
  color: var(--color-text-muted);
}

.post-title {
  font-family: var(--font-display);
  font-size: clamp(1rem, 1.8vw, 1.15rem);
  font-weight: 400;
  line-height: 1.3;
  margin: 0;
  color: var(--color-heading);
}
.post-title a {
  color: inherit;
  text-decoration: none;
  transition: color var(--transition);
}
.post-title a:hover { color: var(--color-accent-dark); }

.post-meta {
  display: flex;
  align-items: center;
  gap: 6px;
  font-size: 0.72rem;
  color: var(--color-text-faint);
  letter-spacing: 0.03em;
  margin: 0;
}

.no-results {
  text-align: center;
  padding: 80px 0;
  color: var(--color-text-muted);
  font-size: 1rem;
}

/* ── Pagination ── */
.pagination {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  margin-top: 56px;
  padding-top: 32px;
  border-top: 1px solid var(--color-border);
}

.page-btn {
  display: flex;
  align-items: center;
  gap: 6px;
  background: none;
  border: 1px solid var(--color-border);
  padding: 10px 18px;
  font-family: var(--font-body);
  font-size: 0.78rem;
  color: var(--color-text-muted);
  cursor: pointer;
  transition: all var(--transition);
  -webkit-tap-highlight-color: transparent;
  min-height: 42px;
}
.page-btn:hover:not(:disabled) {
  border-color: var(--color-ink);
  color: var(--color-ink);
}
.page-btn:disabled {
  opacity: 0.35;
  cursor: not-allowed;
}

.page-numbers {
  display: flex;
  align-items: center;
  gap: 4px;
}

.page-num {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 38px;
  height: 38px;
  border: 1px solid var(--color-border);
  background: none;
  font-family: var(--font-body);
  font-size: 0.82rem;
  color: var(--color-text-muted);
  cursor: pointer;
  transition: all var(--transition);
  -webkit-tap-highlight-color: transparent;
}
.page-num:hover { border-color: var(--color-ink); color: var(--color-ink); }
.page-num.active {
  background: var(--color-ink);
  border-color: var(--color-ink);
  color: var(--color-white);
  font-weight: 600;
}

.page-ellipsis {
  font-size: 0.82rem;
  color: var(--color-text-faint);
  padding: 0 4px;
  pointer-events: none;
}

.page-info {
  text-align: center;
  font-size: 0.75rem;
  color: var(--color-text-faint);
  margin-top: 16px;
  letter-spacing: 0.02em;
}

/* ── Responsive ── */
@media (max-width: 860px) {
  .posts-grid { grid-template-columns: repeat(2, 1fr); gap: 32px 20px; }
}

@media (max-width: 480px) {
  .read-hero { padding: 56px 20px 40px; }
  .posts-section { padding: 40px 16px 72px; }
  .posts-grid { grid-template-columns: repeat(2, 1fr); gap: 24px 14px; }
  .filter-tab { padding: 14px 16px; }
  .page-btn span { display: none; }
  .page-btn { padding: 10px 12px; }
}
</style>

<script define:vars={{ POSTS_PER_PAGE }}>
const tabs        = document.querySelectorAll('.filter-tab');
const allItems    = Array.from(document.querySelectorAll('.post-item'));
const noResults   = document.getElementById('no-results');
const searchInput = document.getElementById('post-search');
const clearBtn    = document.getElementById('search-clear');
const prevBtn     = document.getElementById('prev-btn');
const nextBtn     = document.getElementById('next-btn');
const pageNumbers = document.getElementById('page-numbers');
const pageInfo    = document.getElementById('page-info');

let activeCat  = 'All';
let searchQ    = '';
let currentPage = 1;
let filteredItems = [];

function getFilteredItems() {
  return allItems.filter(item => {
    const catMatch  = activeCat === 'All' || item.dataset.category === activeCat;
    const keyword   = searchQ.trim().toLowerCase();
    const textMatch = !keyword ||
      (item.dataset.title  ?? '').includes(keyword) ||
      (item.dataset.desc   ?? '').includes(keyword) ||
      (item.dataset.category ?? '').toLowerCase().includes(keyword);
    return catMatch && textMatch;
  });
}

function renderPage() {
  filteredItems = getFilteredItems();
  const total = filteredItems.length;
  const totalPages = Math.max(1, Math.ceil(total / POSTS_PER_PAGE));
  if (currentPage > totalPages) currentPage = totalPages;

  const start = (currentPage - 1) * POSTS_PER_PAGE;
  const end   = start + POSTS_PER_PAGE;

  // Show/hide items
  allItems.forEach(item => item.classList.remove('visible'));
  filteredItems.slice(start, end).forEach(item => item.classList.add('visible'));

  // No results
  if (noResults) noResults.hidden = total > 0;

  // Pagination controls
  const pagination = document.getElementById('pagination');
  if (pagination) pagination.style.display = totalPages <= 1 && total > 0 ? 'none' : 'flex';

  if (prevBtn) prevBtn.disabled = currentPage <= 1;
  if (nextBtn) nextBtn.disabled = currentPage >= totalPages;

  // Page number buttons
  if (pageNumbers) {
    pageNumbers.innerHTML = '';
    if (totalPages > 1) {
      renderPageNumbers(totalPages);
    }
  }

  // Info text
  if (pageInfo) {
    if (total === 0) {
      pageInfo.textContent = '';
    } else {
      const from = start + 1;
      const to   = Math.min(end, total);
      pageInfo.textContent = `Showing ${from}–${to} of ${total} post${total !== 1 ? 's' : ''}`;
    }
  }

  // Scroll to top of grid on page change
  const grid = document.getElementById('posts-grid');
  if (grid && currentPage > 1) {
    grid.scrollIntoView({ behavior: 'smooth', block: 'start' });
  }
}

function renderPageNumbers(totalPages) {
  const makeDot = () => {
    const span = document.createElement('span');
    span.className = 'page-ellipsis';
    span.textContent = '…';
    pageNumbers.appendChild(span);
  };
  const makeBtn = (n) => {
    const btn = document.createElement('button');
    btn.className = `page-num${n === currentPage ? ' active' : ''}`;
    btn.textContent = n;
    btn.setAttribute('aria-label', `Page ${n}`);
    if (n === currentPage) btn.setAttribute('aria-current', 'page');
    btn.addEventListener('click', () => { currentPage = n; renderPage(); });
    pageNumbers.appendChild(btn);
  };

  if (totalPages <= 7) {
    for (let i = 1; i <= totalPages; i++) makeBtn(i);
  } else {
    makeBtn(1);
    if (currentPage > 3) makeDot();
    const start = Math.max(2, currentPage - 1);
    const end   = Math.min(totalPages - 1, currentPage + 1);
    for (let i = start; i <= end; i++) makeBtn(i);
    if (currentPage < totalPages - 2) makeDot();
    makeBtn(totalPages);
  }
}

function setCategory(cat) {
  activeCat = cat;
  currentPage = 1;
  tabs.forEach(t => {
    const isActive = (t.dataset.category ?? 'All') === cat;
    t.classList.toggle('active', isActive);
    t.setAttribute('aria-selected', String(isActive));
  });
  renderPage();
}

// Category tabs
tabs.forEach(tab => {
  tab.addEventListener('click', () => setCategory(tab.dataset.category ?? 'All'));
});

// Prev / Next
prevBtn?.addEventListener('click', () => {
  if (currentPage > 1) { currentPage--; renderPage(); }
});
nextBtn?.addEventListener('click', () => {
  const total = getFilteredItems().length;
  const totalPages = Math.ceil(total / POSTS_PER_PAGE);
  if (currentPage < totalPages) { currentPage++; renderPage(); }
});

// Search
searchInput?.addEventListener('input', () => {
  searchQ = searchInput.value;
  currentPage = 1;
  if (clearBtn) clearBtn.hidden = !searchQ;
  renderPage();
});

clearBtn?.addEventListener('click', () => {
  if (searchInput) searchInput.value = '';
  searchQ = '';
  currentPage = 1;
  clearBtn.hidden = true;
  searchInput?.focus();
  renderPage();
});

// URL category param
const urlCat = new URLSearchParams(window.location.search).get('category');
if (urlCat) {
  setCategory(urlCat);
} else {
  renderPage();
}
</script>
